/** @type {import('next').NextConfig} */
// const nextConfig = {};
// next.config.js

const nextConfig = {
    images: {
      domains: ['images.unsplash.com'],
    },
  };

  
export default nextConfig;
